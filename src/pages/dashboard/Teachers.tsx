import { PageCaption } from "../../components";

const Teachers = () => {
  return (
    <div className="p-5">
      <PageCaption title="Ustozlar" count={10} />
    </div>
  );
};

export default Teachers;
